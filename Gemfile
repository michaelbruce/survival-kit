source 'https://rubygems.org'

ruby '2.2.0'

gem 'git-up'
gem 'parser', require: false # Rubocop works better with up to date parse gem
gem 'rubocop', require: false # Static code analyser, works with emacs flycheck
gem 'jekyll', require: false

group :development do
  gem 'pry'
  gem 'pry-rescue'
  gem 'pry-stack_explorer'
  gem 'brakeman', require: false
end

# Required for data-reader
gem 'roo-xls', require: false
gem 'roo', require: false

# Required for charter
gem 'chartkick', require: false
gem 'sinatra', require: false
gem 'gruff', require: false
gem 'nokogiri', require: false

# Old gems
# gem 'thyme', require: false # Pomodoro timer that integrates with tmux/vim
# gem 'seeing_is_believing', require: false # originally used in emacs
# gem 'ruby-lint'
